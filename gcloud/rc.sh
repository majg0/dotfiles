# The next line updates PATH for the Google Cloud SDK.
if [ -f "$GCLOUD_DATA_DIR/google-cloud-sdk/path.zsh.inc" ]; then . "$GCLOUD_DATA_DIR/google-cloud-sdk/path.zsh.inc"; fi

# The next line enables shell command completion for gcloud.
if [ -f "$GCLOUD_DATA_DIR/google-cloud-sdk/completion.zsh.inc" ]; then . "$GCLOUD_DATA_DIR/google-cloud-sdk/completion.zsh.inc"; fi
